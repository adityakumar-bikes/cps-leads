# Google service account key — NEVER commit this
credentials.json
*.json.key
service_account*.json

# Python
__pycache__/
*.pyc
.env
venv/
.venv/

# macOS
.DS_Store

# Large leads cache — stored in GitHub Actions cache, not the repo
data/all_leads.json.gz
